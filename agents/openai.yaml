interface:
  display_name: "合并已映射疾病行"
  short_description: "校验 Mapping 后底稿，逐组批准并生成两层静态合并结果"
  default_prompt: "Use $merge-mapped-disease-rows to validate my finalized Mapping workbook, preview two-layer candidates, close every cross-source decision, and generate a static merged sheet."
